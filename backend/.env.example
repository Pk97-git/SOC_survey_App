# Copy this file to .env and fill in your values
# NEVER commit .env to source control

JWT_SECRET=CHANGE_THIS_to_a_long_random_secret_at_least_64_chars
JWT_EXPIRES_IN=24h

# Comma-separated list of allowed origins (mobile app scheme, web dashboard URL)
ALLOWED_ORIGINS=exp://,http://localhost:8081,http://localhost:3000

NODE_ENV=production
